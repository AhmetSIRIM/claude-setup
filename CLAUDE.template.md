<!-- Personal instructions, loaded into every session. Copy this file to
     ~/.claude/CLAUDE.md and write it your way; there is no required structure.
     Typical content: who you are, the language and shape of answers you want,
     code style preferences, what feedback Claude should surface. The filled
     copy stays local and is never tracked here. HTML comments are stripped
     before the content reaches Claude's context, and the session-start doctor
     warns while the copy has nothing of your own in it yet. -->

Working rules:
- Detailed working rules live in `~/.claude/rules/`; they load with this file
